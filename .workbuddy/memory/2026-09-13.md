# 2026-09-13 工作日志

## LogiTech2 插件修复（Minecraft Slimefun 附属）

用户报告 3 个问题，全部处理完毕，`mvn package` 编译打包成功：

1. **堆叠机器塞大量机器卡顿**
   - 清理了 AbstractMachine / RecipeSupporter 里遗留的每 tick 刷屏调试日志
   - NetworksQuantumStorageAdapter 增加按 meta 的 QuantumCache 短 TTL 缓存，避免每次查询重复 PDC 反序列化（不改变数量语义，无刷物风险）

2. **量子纠缠绑定网络抽屉只认第一格**
   - ItemStorageCache.getWithoutCheck 改用 `lp.getItemAmount(loc, item)` 按格取数，修复用总量当第一格数量导致的"空手套白狼"超发
   - LocationStorageProxy 新增 `rebindToType`（rebound 标志防重复切换），CraftUtils 在 matchRecipe/matchMultiRecipe/countMultiRecipe/countMultiOutput/multiForcePush 等链路接入 `tryRebindLocationProxy` + `calLocationProxyRoom`
   - LocationProxy 接口新增 getItemAmount/setItemAmount/canAcceptItem 等 per-item 默认方法

3. **逻辑/终极合成工厂 3x3 不显示配方**：确认工作区已有修复（懒加载+VANILLA_CRAFTTABLE+类型变更重算），本次编译验证通过

## 环境备忘
- **JDK 21 在 D 盘**：`D:\Java\jdk-21.0.10+7`（编译用 `JAVA_HOME="D:\Java\jdk-21.0.10+7"`）；.jdks 里只有 temurin-17 不够用（matlib 依赖是 Java 21 字节码）
- Maven 在 `C:\apache-maven-3.9.12\bin\mvn.cmd`
- 产物：`target/LogiTech2.jar`（shaded，~2MB）
- 改动尚未 git commit
